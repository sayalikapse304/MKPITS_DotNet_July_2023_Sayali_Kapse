#include<stdio.h>
void primenumber(int num)
{
	int i;
	for(i=2;i<=num-1;i++)
	{
		if(num % i == 0)
	}
	int main()
	{
		int n, res;
		res=primenumber(n);
		if(res==1)
		printf("%d is a prime number",n);
		else
		printf("%d is not a prime number",n);
	}
